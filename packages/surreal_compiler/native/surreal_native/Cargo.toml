[package]
name = "surreal_native"
version = "0.1.0"
edition = "2024"

[lib]
name = "surreal_native"
crate-type = ["cdylib"]

[dependencies]
# Rustler for NIF bindings
rustler = "0.36"

# Surreal compiler - use the parent crate
surreal = { path = "../../../.." }

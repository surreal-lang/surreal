// JSON serialization utilities for Dream
//
// Note: Derive macros with impl block generation need compiler fixes.
// For now, we provide a ToJson trait that structs implement manually.

pub trait ToJson {
    fn to_json(self) -> any;
}
